object Test {
    const val testInstrumentals = "androidx.test.runner.AndroidJUnitRunner"

    const val unit_junit = "junit:junit:${Versions.junit}"

    const val android_junit = "androidx.test.ext:junit:${Versions.android_junit}"
    const val android_espresso = "androidx.test.espresso:espresso-core:${Versions.android_espresso}"
}